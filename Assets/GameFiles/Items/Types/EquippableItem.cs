using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EquippableItem : Item {

    public EquippableItem(string name, string description, int color) : base(name, description, color)
    {

    }
}
